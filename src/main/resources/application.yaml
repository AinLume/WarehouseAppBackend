ktor:
  application:
    modules:
      - ApplicationKt.module
  deployment:
    port: 8080

postgres:
  host: "localhost"
  port: "5432"
  name: "app_db"
  user: "admin"
  password: "securePassword"

openapi:
  spec: "openapi/docs-v1.yml"

jwt:
  publicKeyPath: "src/main/resources/keys/publicKey.pem"
  privateKeyPath: "src/main/resources/keys/privateKey.pem"